MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/chatty?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
PORT=8080
HOST=0.0.0.0

# Set in Netlify UI: Site settings → Environment variables
# MONGODB_URI, JWT_SECRET
